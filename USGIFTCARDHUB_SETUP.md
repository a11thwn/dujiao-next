# US Gift Card Hub 搭建与上线清单

本项目直接基于 [Dujiao-Next](https://github.com/dujiao-next/dujiao-next) 构建，保留其 Git 历史、`LICENSE`（GPLv3）、前台页脚署名和官方文档入口。上游使用与部署说明见 [Dujiao-Next 官方文档](https://dujiao-next.com)。

## 当前已完成

- Go 后端、用户前台和管理后台已部署到 VPS200，并由 systemd 托管运行。
- SQLite 数据库已初始化；本地生产数据目录为 `db/`，该目录不会提交到 Git。
- 站点名称、美元币种、英文默认语言（保留简体中文/繁体中文切换）、基础 SEO、关于、条款和隐私草案已设置。
- 移除重复的顶部和页脚自定义链接，仅保留前台最底部原生的 Dujiao-Next 项目署名。
- 已按 `card.pdf`（24 页、编号 1–100）建立“美国礼品卡”根分类、9 个浏览分类和 99 个唯一品牌商品；PDF 的编号 28、98 均为 Lane Bryant，已去重并保留来源编号。
- 已识别并整理 PDF 中的商品介绍，为 99 个商品补充英文详情文案；商品卡片与详情页均使用对应品牌礼品卡图片作为主图。
- PDF 中展示的可输入金额区间和固定下拉面额已作为目录信息显示；固定下拉面额使用 `$25/50/100` 形式。它们没有被误当作单一售价。
- 所有目录商品保留零库存作为后端下单保护，但前台不显示“售罄”状态；卡片和详情正常展示金额，购买按钮禁用且只显示暖色购物车图标。首次打开不显示公告弹窗。
- 首页分类卡和商品分类侧栏显示实际可见商品数量。
- `usgiftcardhub.com` 已启用 HTTPS；Cloudflare 将 `www` 301 到裸域并保留路径与查询参数。
- 当前关闭 Redis、异步队列、SMTP 和支付渠道，生产站仅用于目录浏览与搭建验收。

## 上线前必须提供或确认

| 范围 | 需要的信息 | 当前状态 |
| --- | --- | --- |
| 域名与服务器 | 正式域名、VPS/云服务器、DNS 管理位置、是否使用 Cloudflare/CDN | 已部署到 VPS200；`usgiftcardhub.com` HTTPS 和 Cloudflare `www` 301 已生效 |
| 商城主体 | 运营主体名称、所在国家/地区、客服联系邮箱、Telegram/WhatsApp、客服时间 | 待提供 |
| 品牌素材 | Logo、favicon、社交分享图、品牌色、中文/英文正式名称 | 待提供 |
| 商品与货源 | 每个品牌的成本价、实际售价、地区限制、货源方式、售后边界；复核 PDF 金额是否就是最终可售面额 | 已有 99 品牌和 PDF 金额目录，交易信息待确认 |
| 交付方式 | 卡密自动交付、人工交付或上游接口；如自动交付，需要真实卡密或供应商 API | 待确认 |
| 支付 | Stripe、PayPal、支付宝、微信、USDT 或其他渠道；商户号、密钥、回调域名 | 未配置，前台购买入口保持禁用 |
| 邮件 | SMTP 主机、端口、账号、密码、发件人名称和邮箱 | 未配置 |
| 用户规则 | 是否允许游客购买、是否开放注册、是否强制邮箱验证、是否启用 Google/Telegram 登录 | 临时允许注册且不验证邮箱 |
| 合规文本 | 正式服务条款、隐私政策、退款政策、数字商品免责声明、税务/发票说明 | 当前为草案 |
| 运营通知 | 新订单、支付、退款、低库存通知发送到哪里 | 未配置 |
| 统计与营销 | Google Analytics、Meta Pixel 或其他统计脚本及 Cookie 规则 | 未配置 |

## 生产安全与运维

正式部署时必须重新生成三组互不相同的应用密钥，并替换本地管理员密码；后台路径应保持为不易猜测的路径。建议启用管理员 TOTP 2FA、Redis/异步队列、HTTPS、可信反向代理范围、登录验证码、数据库与上传目录备份、日志轮转和恢复演练。

SQLite 适合初期单机部署；如果后续有多实例或更高并发，再迁移到 PostgreSQL。支付回调、邮件发送和自动交付必须在测试环境完整验证后才能开放真实支付。

## 重放目录配置

如需从原始 PDF 重新生成商品品牌图片，先执行：

```bash
node scripts/extract-card-images.mjs /absolute/path/to/card.pdf
```

在目标 Dujiao-Next API 已启动且管理员已创建后执行：

```bash
DJ_ADMIN_USER='<管理员账号>' \
DJ_ADMIN_PASSWORD='<管理员密码>' \
DJ_API_BASE='https://你的域名/api/v1' \
DJ_SITE_URL='https://你的域名' \
node scripts/bootstrap-usgiftcardhub.mjs
```

该脚本会更新站点设置，并按 slug 创建或更新 PDF 目录分类与商品。PDF 金额只作为可见的目录信息，不会成为单一售价；正式上线前必须在后台确认最终 SKU/面额、价格、库存、商品图与交付说明，再接入并测试支付渠道。
