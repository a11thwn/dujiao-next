# CHANGELOG

此文件用于记录项目的重要修改，按时间倒序维护。

---

## [2026-08-25] - 建立双 VPS 温备与 Google Drive 异地备份

### 问题描述

- US Gift Card Hub 仅部署在 VPS200，主机或站点故障时缺少可直接接管的完整副本。
- SQLite、Redis 队列、应用配置、上传文件、服务单元和 TLS 恢复材料没有统一的一致性备份流程。
- 生产数据尚未形成经过加密与恢复验证的 Google Drive 异地归档。

### 分析原因

- 普通目录复制无法保证 WAL 模式 SQLite 与 Redis 队列处于一致可恢复状态。
- 同时运行多个 Worker 会造成重复发信、重复消费或订单状态分叉，因此备用机不能作为并行写节点常驻运行。
- 大文件通过 Google Drive 连接器中转时存在超时限制，需要保留浏览器上传与元数据回读的备用路径。

### 解决方案

- 将 VPS201、VPS228 建成单主写入架构下的温备节点，完整保存应用、SQLite、Redis、配置、systemd 和 TLS 材料。
- VPS200 每 15 分钟生成 SQLite 在线备份与 Redis RDB，并通过受限的非 root 同步账号增量发送到两台温备。
- 温备每 5 分钟校验并应用完整快照，API、Worker、Redis 默认保持停止，接管时才按顺序启动并验证 HTTPS。
- 每日生成 AES-256-CBC + PBKDF2 加密归档，上传 Google Drive 后按文件名和字节数回读验证；备份密钥不进入仓库或 Drive。

### 改动内容

- 新增 `deploy/failover/primary-snapshot.sh`：一致性快照、双机推送、加密归档、解密验包和本地轮转。
- 新增 `standby-apply.sh`、`standby-activate.sh`、`standby-deactivate.sh`：副本应用、单写确认接管、隔离演练与撤回。
- 新增 `restore-encrypted-archive.sh`：整包 SHA-256、归档路径、内部文件校验与停止状态恢复。
- 新增主站快照/归档和温备应用的 systemd service/timer，共 6 个单元文件。
- 新增 `deploy/failover/README.md`，记录拓扑、接管、撤回、演练、Drive 恢复和日常核验命令。
- 已在 VPS201、VPS228 完成数据库/二进制/TLS 哈希核对和隔离 HTTPS 接管演练；VPS201 另完成一次加密归档恢复演练。
- 已创建 Google Drive `USGiftCardHub Backups` 目录，首份加密归档大小为 `59,819,504` 字节，并配置每日上传自动任务。

### 影响范围

- VPS200 继续作为唯一生产写节点；现有 API、Worker、Redis 与公网流量保持不变。
- VPS201、VPS228 可通过一条接管命令启动，当前 DNS 切换仍需在 Cloudflare 手动完成，避免误判造成双主。
- 快照目标 RPO 约 20 分钟以内；实际 RTO 取决于接管检查和 Cloudflare DNS 切换。
- 服务器加密归档保留 14 天，温备 SQLite 回滚点最多保留 16 个。

### 后续计划

- 持续检查每日 Google Drive 自动任务和三台主机快照编号是否一致。
- 定期执行隔离接管与 Drive 归档恢复演练，确认新增数据与路由仍被完整覆盖。

## [2026-08-25] - 配置商品运营账号的生产后台权限

### 问题描述

- 新增的 `Suzy_admin` 与 `wen_admin` 管理员账号尚未分配角色，无法保存或使用后台业务权限。
- 两个账号需要开放大部分运营功能，但支付渠道与系统管理只能查看，不能修改。

### 分析原因

- 两个非超级管理员账号创建后没有 Casbin 角色绑定。
- 内置角色按业务域整体授权，直接组合会同时开放支付渠道修改和系统设置修改，无法满足当前只读边界。
- Casbin 当前采用仅允许策略，没有可用于抵消内置写权限的拒绝规则，因此需要独立的自定义角色。

### 解决方案

- 在生产权限库中创建并维护自定义角色 `product_order_operator`，按当前内置权限清单生成 186 条直接策略。
- 除支付渠道和系统管理外开放其他后台业务权限；支付渠道与系统管理仅保留 `GET` 查看权限。
- 保留管理员个人修改密码和管理 2FA 的自助权限。
- 将该角色分别绑定到 `Suzy_admin`（ID 2）与 `wen_admin`（ID 3）。

### 改动内容

- 更新 VPS200 `/opt/usgiftcardhub/db/dujiao.db` 中的 Casbin 策略与管理员角色绑定。
- 禁止两个账号新增、编辑、删除或测试支付渠道。
- 禁止两个账号修改系统设置、权限、升级与重启、渠道客户端及 Telegram 配置。
- 变更前生成 `/opt/usgiftcardhub/backups/before-broad-readonly-system-role-20260825T035849Z.db` 备份。
- 重启 `usgiftcardhub.service` 使权限策略加载生效，并完成数据库与运行状态复核。

### 影响范围

- `Suzy_admin` 与 `wen_admin` 重新登录后可使用除上述只读边界外的其他后台功能。
- 超级管理员及其他角色不受影响。
- 支付渠道和系统管理页面可查看，但相关写操作不会获得授权。

### 后续计划

- 后台新增管理接口或调整路由权限后，重新核对自定义角色的权限覆盖与只读边界。
- 定期复查两个运营账号的实际职责，继续按最小必要权限收敛敏感操作。

## [2026-08-13] - 搭建并部署 US Gift Card Hub 礼品卡目录

### 问题描述

- 原始 Dujiao-Next 商城尚未配置美国礼品卡品类、商品资料和品牌图片。
- 支付与交付尚未接入，但前台仍需要完整展示商品面额并明确阻止购买。
- 默认语言、公告弹窗、库存状态和商品卡片样式不符合当前英文目录站的展示需求。

### 分析原因

- `card.pdf` 中的 100 条来源记录尚未结构化，其中 Lane Bryant 重复出现一次。
- 原始前台按价格和库存驱动购买流程，不支持“仅展示可选面额、暂不交易”的目录模式。
- 站点缺少针对 VPS200、TLS 分流和生产密钥初始化的部署配置。

### 解决方案

- 将 PDF 品牌、品类、面额和介绍整理为可重复执行的初始化目录，并为 99 个唯一商品生成对应品牌卡图片。
- 为目录商品增加面额展示与目录预览判断，隐藏售罄提示、数量和规格控件，禁用购买入口并只保留暖色购物车图标。
- 将英文设为默认语言，关闭首次公告弹窗，并为首页分类卡及分类侧栏增加实际商品数量。
- 增加 VPS200 的 systemd、应用配置和 TLS vhost 部署模板，生产密钥、管理员密码和后台路径均在安装时生成。

### 改动内容

- 新增 `scripts/card-catalog.mjs`、`scripts/extract-card-images.mjs` 和 `scripts/bootstrap-usgiftcardhub.mjs`，支持从 PDF 重建图片并幂等写入站点设置、9 个分类和 99 个商品。
- 新增 `frontend/user/public/gift-cards/` 下的 99 张 WebP 品牌卡图片。
- 更新普通与 Vault 前台的首页、商品列表、商品详情、快捷购买和移动端购买栏。
- 新增分类商品数量统计，并在首页分类卡和分类侧栏显示数量。
- 删除重复的顶部 Open Source 菜单和自定义页脚链接，保留前台原生 Dujiao-Next 署名。
- 新增 `USGIFTCARDHUB_SETUP.md` 和 `deploy/vps200/` 部署资料。
- 已将生产站部署到 `https://usgiftcardhub.com`，并验证 Cloudflare 的 `www` 301 跳转。

### 影响范围

- 用户前台默认以英文展示 99 个美国礼品卡品牌及 PDF 中的可用金额。
- 当前不支持下单、支付和交付；所有购买入口保持禁用。
- 管理后台、公开 API、原生 Dujiao-Next 署名和现有多语言切换继续保留。
- VPS200 上原有 TLS 分流和其他服务不受影响。

### 后续计划

- 补充运营主体、客服渠道、正式条款、隐私与退款政策。
- 确认 SKU、售价、库存和供应商交付方式后，再接入并验收支付渠道。
